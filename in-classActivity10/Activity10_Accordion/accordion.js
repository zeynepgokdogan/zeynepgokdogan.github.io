$(document).ready(function () {
  $("#accordion").accordion({
    collapsible: true, // Allows the currently open panel to be closed again
    active: false, // All panels will be closed initially
  });
});
